#!/bin/sh

set -e

trap 'rm -f diag-avg-two.input diag-avg-two.output' EXIT
cat > diag-avg-two.input <<EOF
-8.5
4
EOF
echo '-2.25' > diag-avg-two.output
@DIAG_AVG@ < diag-avg-two.input | diff -u diag-avg-two.output -
